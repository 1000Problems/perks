# Postgres connection string. Use the pooled URL from your Neon project:
# Neon dashboard → your project → Connection details → "Pooled connection".
# Make sure the URL ends with ?sslmode=require.
DATABASE_URL=
